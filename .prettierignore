# Build output & deps (Prettier ignores node_modules by default; listed for clarity)
.next/
out/
build/
coverage/
node_modules/
dist/
packages/*/dist/
packages/*/server/

# Generated / vendored
next-env.d.ts
*.tsbuildinfo
public/
gpt/

# Transient bundles the mcp-server tests build beside their sources (the Jest
# globalSetup builds the IV worker; the process-exit test builds its child).
# Gitignored in packages/mcp-server/.gitignore, absent from a clean checkout.
packages/mcp-server/src/utils/iv-solver-worker.js
packages/mcp-server/src/utils/iv-solver-worker.js.map
packages/mcp-server/src/utils/iv-solver-pool-exit-child.built.mjs

# Lockfile
package-lock.json

# Deliberately malformed fixtures (must stay invalid for the tests that parse them)
packages/mcp-server/tests/fixtures/*.malformed.json
